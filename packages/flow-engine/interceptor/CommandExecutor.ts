import { Command } from './Command'
import { CommandConfig } from './CommandConfig'

export class CommandExecutor {
  execute(config: CommandConfig, command: Command) {
    //
  }

  setNext(next: CommandExecutor) {
    //
  }

  getNext() {
    //
  }
}
